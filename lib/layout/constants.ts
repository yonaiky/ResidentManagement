/** Ancho máximo del contenido (Stripe/Vercel-style, ancho generoso en pantallas grandes). */
export const PAGE_MAX_WIDTH = "100rem";

/** Altura del header premium (debe coincidir con header.tsx). */
export const HEADER_HEIGHT_PX = 72;
